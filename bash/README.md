# Bash

The little bash scripts I am playing around with and using to apply best practices and be mindful of security.

I always run `shellcheck` before making the script executable to catch bugs early.
